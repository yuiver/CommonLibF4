target("commonlibf4-ng-addresslibgen")
    -- set build by default
    set_default(false)

    -- set build group
    set_group("commonlibf4-ng/tool")

    -- add source files
    add_files("src/**.cpp")
